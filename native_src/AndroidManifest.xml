<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              Deep links. Shared daily results (Chordle, Tonale, Diadle) carry an
              https link to a bounce page, which tries intonare://MODULE and falls
              back to the Play listing if nothing intercepts it. This is the half
              that intercepts.

              A custom scheme, not App Links, for now. App Links need the release
              keystore's SHA-256 in an assetlinks.json served from the domain, and
              the app verified against it. The shared URL is https either way, so
              adding App Links later is purely additive; no change to the app's
              behaviour, and no change to any link already sent.

              launchMode="singleTop" above means a link arriving while the app is
              already open is delivered to onNewIntent rather than starting a second
              activity. Capacitor forwards that to the appUrlOpen listener.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="intonare" />
            </intent-filter>

            <!--
              App Links: the https URL opens the app DIRECTLY, with no browser and no
              tap. autoVerify makes Android fetch
              https://lieutenantdan76.github.io/.well-known/assetlinks.json on install
              and check this app's signing certificate against it. Once verified, the
              OS owns the URL and the browser never sees it.

              This is the whole point. Chrome refuses to launch an app from a page
              without a user gesture, so the bounce page can only ever be a
              tap-to-open. App Links sidestep that entirely: there is no page, no
              script, and therefore no gesture rule.

              go.html stays, and stays useful: it is what people WITHOUT the app get.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="lieutenantdan76.github.io"
                      android:pathPrefix="/Intonare/go.html" />
            </intent-filter>
        </activity>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="com.android.vending.BILLING" />
</manifest>
